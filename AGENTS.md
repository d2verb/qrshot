# AGENTS.md

qrshot の開発ガイド。人間にもエージェントにも向けて書いてある。

## このツールは何か

`qrshot` と打つと画面の領域選択が始まり、選択範囲に写っている QR コードをデコードしてクリップボードにコピーし、結果を通知する macOS CLI。使い方は [README.md](README.md) を参照。

## コマンド

```bash
swift build          # ビルド
swift test           # テスト
swift build -c release --arch arm64 --arch x86_64   # 配布用 universal binary
```

universal binary の出力先はハードコードせず `swift build -c release --arch arm64 --arch x86_64 --show-bin-path` から取ること。単一アーキテクチャのときとパスが違う。

## ⚠️ 自動化で絶対にやってはいけないこと

エージェントが最初に読むべき節。**どちらも画面を奪って人間待ちになり、セッションが固まる。**

- **`screencapture` を実行しない。** `-i` は十字カーソルで画面を占拠し、人間がドラッグするまで返らない。`-R` や `-l` などの非対話形式も画面に触れ、権限ダイアログを誘発しうる。
- **引数なしの `qrshot` を実行しない。** 上と同じ理由。安全なのは `--help`、`--version`、不明な引数、そして `terminal-notifier` が PATH に無い状態での実行（事前チェックで落ちる）だけ。

領域選択の動作確認が必要なら、自分で実行せず人間に依頼すること。

## 設計

ロジックはすべてライブラリターゲット `QRShotKit` にある。実行ファイルターゲット `qrshot` は、具体実装の組み立てと exit code への変換だけを担う。

外部プロセスやシステム状態に触る関心事を、それぞれプロトコルで切り出している。

| プロトコル | 実装 | 触るもの |
|---|---|---|
| `ScreenCapturer` | `InteractiveScreenCapturer` | `screencapture` プロセス |
| `QRDecoder` | `VisionQRDecoder` | Vision |
| `Clipboard` | `PasteboardClipboard` | `NSPasteboard` |
| `Notifier` | `TerminalNotifier` | `terminal-notifier` プロセス |

`QRShot` オーケストレータはこの 4 つのプロトコルだけに依存し、具体実装を一切知らない。**この性質は `QRShot.swift` が import を 1 つも持たないことで確認できる。** 壊れていないかはそこを見れば分かる。

外部境界を新しく足すときは、同じ形にすること。プロトコル + エラー enum（`Error, Equatable, CustomStringConvertible`、`description` は日本語のユーザー向け文言）+ 実装を 1 ファイルにまとめ、オーケストレータからは fake を差して経路をテストする。

exit code、usage、stderr の書式、具体実装の選択は `main.swift` だけが知っている。`QRShotKit` にこれらを持ち込まないこと。

## 規約

- **コメントとテストの表示名は日本語。** 型名・関数名は英語。
- **外部 Swift パッケージ依存はゼロ。** 実行時依存は `terminal-notifier` のみ。この 2 つは維持する価値がある制約として扱う。
- **コミットメッセージは Gitmoji 前置き。**
- **ビルド警告ゼロを保つ。** CI では強制していないので、ローカルで確認すること。
- テストの fake は 2 種類に分ける。`Stub*` は構築時に振る舞いを固定するもの、`Spy*` は起きたことを記録し、失敗を可変プロパティで後から差し込むもの。
- ユーザーに見える日本語文言（通知メッセージなど）は仕様。言い換えたり空白を足したりしない。

## 踏み抜いた罠

**すべて実際にこのリポジトリで発生し、修正済み。** 同じ場所を触るときは思い出すこと。それぞれ退行防止テストが入っている。

### `CGImageSourceCreateWithURL` の画像は元ファイルを消すと死ぬ

URL から作った image source が返す `CGImage` は画素をファイルから遅延読み込みする。一時ファイルを `defer` で消してからその画像を返すと、受け取った側では画素が読めない。**QR がきれいに写っていても必ず「読み取れない」になり、ツールが端から端まで壊れる。** 静かに壊れるのでビルドもテストも通ってしまう。

先に `Data` へ読み切ってから `CGImageSourceCreateWithData` を使うこと。

### stderr への `FileHandle.write` はプロセスを落とす

`FileHandle.write(_:)` は書き込み失敗時に `NSException` を投げる Objective-C メソッドで、stderr が閉じられていると SIGABRT（exit 134）になる。**成功パスでも起きる** — クリップボードへのコピーも通知も終わったあとに複数検出の注記を書こうとして落ち、成功した実行が 134 を返す。`fputs` を使うこと。

### `NSPasteboard` は並行アクセスに耐えない

名前の違うペーストボードを使っていても AppKit 内部の共有状態でレースし、`stringForType:` が `objc_msgSend` で不正ポインタ参照を起こしてテストプロセスごと落ちる。実測でスイート全体の並行実行の約 1 割。ペーストボードを触るテストスイートには `.serialized` を付ける。製品コードは `NSPasteboard.general` を 1 回触るだけなので、これはテスト固有の問題。

### `try?` は Optional を返す throwing 関数で平坦化する

`try?` を `T?` を返す throwing 関数に付けると **`T??` ではなく `T?` に平坦化され、警告なしにコンパイルが通る。** `capture()` でこれをやると `screencapture` の起動失敗が「キャンセル」に化け、無言のまま exit 0 になる。オーケストレータのエラー伝播テストはこの落とし穴を固定するためにある。消さないこと。

### `isExecutableFile` はディレクトリにも true を返す

0755 のディレクトリが実行可能ファイルとして通ってしまう。PATH 上の実行ファイルを探すときは `fileExists(atPath:isDirectory:)` と併用すること。

### Vision の byte → string 変換は UTF-8 検証より寛容

バイナリペイロードの QR をテストで作るとき、`0xFF` の並びは Latin-1 として `"ÿÿÿ..."` にデコードされてしまい `payloadString` が nil にならない。先行バイトのない継続バイト（`0x80`〜`0x87` など）を使うこと。

### クリップボード書き込み失敗は通知されない

意図的な設計。既存の失敗通知を流用すると「QR が読めなかった」と嘘をつくことになり、正しく伝えるには 3 つ目のメッセージが要る。仕様のメッセージは 2 つだけと決まっているので、stderr と exit 2 に留めている。判断の経緯は設計書にある。

## テスト

`swift test` で全部走る。テストは実物を使う。QR 画像は `CIFilter.qrCodeGenerator()` でテスト内に生成するので、フィクスチャ画像をリポジトリに置かない。クリップボードは `NSPasteboard.withUniqueName()` を注入するので、ユーザーの実クリップボードを壊さない。

**このプロジェクトは変異テストを基準にしている。** 「この行を壊してもテストが緑のままなら、その行は固定されていない」という判断でテストを足してきた。テストを追加・変更するときは、意図した変異で実際に落ちることを確認すること。逆に、レビューで「この行は未固定」と指摘されたら、まずその変異を作って再現してから直すこと。

### 自動テストしないもの

- `InteractiveScreenCapturer.capture` — 人間のドラッグが要る。ただし画像を読み込む部分は `loadImage` に切り出してあり、そこはテストされている
- `TerminalNotifier.notify` のプロセス起動 — 通知センターの目視が要る。PATH から実行ファイルを探す `locate` はテストされている
- Vision の検出精度（傾き・低コントラスト・小さい QR）— Apple の実装を測ることになるため

## 手動確認

上記のとおり自動化できない部分がある。リリース前に人間が確認する。

`terminal-notifier` がインストールされ、かつ通知が許可されている必要がある。どちらか欠けていると読み取り自体が成功しても exit 2 になるため、結果を判定できない。

1. **読み取り成功** — QR を画面に表示して `qrshot` を実行し、囲む。成功通知が出て、`pbpaste` が中身を返し、exit 0
2. **読み取り失敗** — QR の無い無地の領域を囲む。失敗通知が出て exit 1
3. **キャンセル** — 先に `echo canary | pbcopy` してから実行し、Esc を押す。通知が出ず exit 0、`pbpaste` が `canary` のまま（クリップボードに触っていない証拠）
4. **ウィンドウ選択** — 実行してスペースキーを押し、QR が表示されているウィンドウをクリック。読み取れること
5. **複数検出** — 大きさが明確に違う QR を 2 つ並べ、両方が入る範囲を囲む。大きいほうがコピーされ、stderr に注記が出ること

引数まわり（`--help` / `--version` / 不明な引数 / `terminal-notifier` 不在）は人間を必要としないので、CI と `env PATH= qrshot` で確認できる。

## ドキュメントの位置づけ

- `docs/superpowers/specs/` の設計書が**仕様の権威**。終了コードの一覧、既知の制限、採用しなかった案とその理由がある。挙動を変えたらここを更新すること。
- `docs/superpowers/plans/` の計画は**実行済みの履歴**。当時の判断を追うためのもので、現在の仕様ではない。
- `README.md` は使う人向け。開発の話は書かない。
