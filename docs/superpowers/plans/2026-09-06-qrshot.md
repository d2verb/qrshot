# qrshot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `qrshot` と打つと画面の領域選択が始まり、選択範囲の QR コードをデコードしてクリップボードへコピーし、通知で結果を知らせる macOS CLI を作る。

**Architecture:** ロジックはすべてライブラリターゲット `QRShotKit` に置き、実行ファイルターゲット `qrshot` は組み立てと exit code だけを担う。外部プロセス・システム状態に触る 4 つの関心事（画面キャプチャ / QR デコード / クリップボード / 通知）をそれぞれプロトコルで切り出し、`QRShot` オーケストレータはプロトコルだけに依存する。これにより経路のテストは fake の差し替えで完結する。

**Tech Stack:** Swift 6 / SwiftPM / Vision（`DetectBarcodesRequest`）/ AppKit（`NSPasteboard`）/ Swift Testing。外部 Swift パッケージ依存はゼロ。実行時に `terminal-notifier` が必要。

**設計書:** `docs/superpowers/specs/2026-09-06-qrshot-design.md`

---

## 前提

この計画に載っているコードとテストは、すべて macOS 26.5.2 / Swift 6.3.3 の実機でコンパイルと実行を確認済み。写経すれば通る。想定外の失敗が出たら、それは環境差分なので立ち止まって報告すること。

`terminal-notifier` は未インストールでもここまでの全タスクは完走できる（Task 8 の手動確認でのみ必要）。

## File Structure

| ファイル | 責務 |
|---|---|
| `Package.swift` | パッケージ定義。macOS 15+、3 ターゲット |
| `.gitignore` | `.build/`、`.swiftpm/` を無視 |
| `Sources/QRShotKit/ScreenCapturer.swift` | `ScreenCapturer` プロトコル、`ScreenCaptureError`、`screencapture -i` 実装 |
| `Sources/QRShotKit/QRDecoder.swift` | `DecodedQR`、`QRDecoder` プロトコル、Vision 実装 |
| `Sources/QRShotKit/Clipboard.swift` | `Clipboard` プロトコル、`ClipboardError`、`NSPasteboard` 実装 |
| `Sources/QRShotKit/Notifier.swift` | `NotificationOutcome`、`Notifier` プロトコル、`NotifierError`、`terminal-notifier` 実装 |
| `Sources/QRShotKit/QRShot.swift` | オーケストレータ。プロトコルだけに依存 |
| `Sources/qrshot/main.swift` | 引数処理、前提チェック、組み立て、exit code |
| `Tests/QRShotKitTests/QRImageFixture.swift` | テスト内で QR 画像を生成・合成するヘルパ |
| `Tests/QRShotKitTests/Fakes.swift` | 4 プロトコルの stub / spy |
| `Tests/QRShotKitTests/VisionQRDecoderTests.swift` | デコーダのテスト |
| `Tests/QRShotKitTests/PasteboardClipboardTests.swift` | クリップボードのテスト |
| `Tests/QRShotKitTests/TerminalNotifierTests.swift` | `locate` のテスト |
| `Tests/QRShotKitTests/QRShotTests.swift` | オーケストレータの経路テスト |
| `README.md` | 使い方、インストール、手動確認手順 |

---

### Task 1: パッケージのスケルトン

**Files:**
- Create: `Package.swift`
- Create: `.gitignore`
- Create: `Sources/qrshot/main.swift`（Task 7 で中身を差し替えるプレースホルダ）
- Create: `Sources/QRShotKit/`, `Sources/qrshot/`, `Tests/QRShotKitTests/`

- [ ] **Step 1: ディレクトリを作る**

```bash
mkdir -p Sources/QRShotKit Sources/qrshot Tests/QRShotKitTests
```

- [ ] **Step 2: `Package.swift` を書く**

```swift
// swift-tools-version: 6.0
import PackageDescription

let package = Package(
    name: "qrshot",
    platforms: [.macOS(.v15)],
    products: [
        .executable(name: "qrshot", targets: ["qrshot"])
    ],
    targets: [
        .target(name: "QRShotKit"),
        .executableTarget(name: "qrshot", dependencies: ["QRShotKit"]),
        .testTarget(name: "QRShotKitTests", dependencies: ["QRShotKit"]),
    ]
)
```

- [ ] **Step 3: `.gitignore` を書く**

```gitignore
.build/
.swiftpm/
```

- [ ] **Step 4: `main.swift` のプレースホルダを置く**

`Package.swift` の `products` が `qrshot` ターゲットを参照しているため、`Sources/qrshot` が空のままだと `swift build` も `swift test` も次のエラーで止まる。

```
error: 'qrshot': target 'qrshot' referenced in product 'qrshot' is empty
```

SwiftPM はパッケージグラフ全体を検証してからコンパイルするので、`--target` や `--filter` で回避できない。**Task 2 以降の各タスクでテストを走らせるために、ここでプレースホルダを置く。** 中身がコメントだけの `main.swift` は空のプログラムとしてリンクが通る。

`Sources/qrshot/main.swift`:

```swift
// Task 7 で本実装に差し替える。
// それまでは、空ターゲットによる SwiftPM のエラーを避けるためのプレースホルダ。
```

`Sources/QRShotKit` と `Tests/QRShotKitTests` は空のままでよい（`products` から参照されていないため警告どまり）。

- [ ] **Step 5: ビルドを確認する**

Run: `swift build`

Expected: `Build complete!`。`Sources/QRShotKit` と `Tests/QRShotKitTests` が空である旨の警告は出るが、エラーは出ない。

- [ ] **Step 6: コミット**

```bash
git add Package.swift .gitignore Sources/qrshot/main.swift
git commit -m "🎉 Set up SwiftPM package skeleton"
```

---

### Task 2: QR デコーダ

**Files:**
- Create: `Tests/QRShotKitTests/QRImageFixture.swift`
- Create: `Tests/QRShotKitTests/VisionQRDecoderTests.swift`
- Create: `Sources/QRShotKit/QRDecoder.swift`

面積が最大の QR を採用する。`payloadStringValue` が取れない QR（バイナリペイロード）は読み取り不能として次の候補へ送る。

- [ ] **Step 1: テスト用の QR 画像ヘルパを書く**

フィクスチャ画像をリポジトリに置かず、テスト内で `CIFilter.qrCodeGenerator()` から生成する。

`Tests/QRShotKitTests/QRImageFixture.swift`:

```swift
import CoreGraphics
import CoreImage
import CoreImage.CIFilterBuiltins
import Foundation

struct FixtureError: Error {
    let message: String
}

/// テスト内で QR 画像を組み立てる。フィクスチャ画像をリポジトリに置かずに済ませるため。
enum QRImageFixture {
    static func qr(_ text: String, scale: CGFloat = 8) throws -> CGImage {
        try qr(Data(text.utf8), scale: scale)
    }

    static func qr(_ payload: Data, scale: CGFloat) throws -> CGImage {
        let filter = CIFilter.qrCodeGenerator()
        filter.message = payload
        filter.correctionLevel = "M"
        guard let output = filter.outputImage else {
            throw FixtureError(message: "QR を生成できませんでした")
        }
        let scaled = output.transformed(by: CGAffineTransform(scaleX: scale, y: scale))
        guard let image = CIContext().createCGImage(scaled, from: scaled.extent) else {
            throw FixtureError(message: "CGImage に変換できませんでした")
        }
        return image
    }

    static func whiteCanvas(
        width: Int,
        height: Int,
        placing placements: [(image: CGImage, origin: CGPoint)] = []
    ) throws -> CGImage {
        guard
            let context = CGContext(
                data: nil,
                width: width,
                height: height,
                bitsPerComponent: 8,
                bytesPerRow: 0,
                space: CGColorSpaceCreateDeviceRGB(),
                bitmapInfo: CGImageAlphaInfo.premultipliedLast.rawValue
            )
        else {
            throw FixtureError(message: "CGContext を作れませんでした")
        }
        context.setFillColor(CGColor(red: 1, green: 1, blue: 1, alpha: 1))
        context.fill(CGRect(x: 0, y: 0, width: width, height: height))
        for placement in placements {
            context.draw(
                placement.image,
                in: CGRect(
                    x: placement.origin.x,
                    y: placement.origin.y,
                    width: CGFloat(placement.image.width),
                    height: CGFloat(placement.image.height)
                )
            )
        }
        guard let image = context.makeImage() else {
            throw FixtureError(message: "画像を書き出せませんでした")
        }
        return image
    }
}
```

- [ ] **Step 2: 失敗するテストを書く**

`Tests/QRShotKitTests/VisionQRDecoderTests.swift`:

```swift
import CoreGraphics
import Foundation
import Testing

@testable import QRShotKit

@Suite("VisionQRDecoder")
struct VisionQRDecoderTests {
    @Test("ASCII の文字列が往復する")
    func decodesASCII() async throws {
        let decoded = try await VisionQRDecoder().decode(QRImageFixture.qr("hello world"))
        #expect(decoded?.payload == "hello world")
    }

    @Test("日本語の文字列が往復する")
    func decodesJapanese() async throws {
        let decoded = try await VisionQRDecoder().decode(QRImageFixture.qr("こんにちは世界"))
        #expect(decoded?.payload == "こんにちは世界")
    }

    @Test("URL が往復する")
    func decodesURL() async throws {
        let url = "https://example.com/path?query=1&other=2"
        let decoded = try await VisionQRDecoder().decode(QRImageFixture.qr(url))
        #expect(decoded?.payload == url)
    }

    @Test("QR が複数あるとき、バウンディングボックスの面積が最大のものを採用する")
    func prefersLargestQR() async throws {
        let big = try QRImageFixture.qr("BIG", scale: 8)
        let small = try QRImageFixture.qr("SMALL", scale: 3)
        let canvas = try QRImageFixture.whiteCanvas(
            width: 400,
            height: 240,
            placing: [(big, CGPoint(x: 8, y: 8)), (small, CGPoint(x: 300, y: 150))]
        )

        let decoded = try await VisionQRDecoder().decode(canvas)

        #expect(decoded?.payload == "BIG")
        #expect(decoded?.detectedCount == 2)
    }

    @Test("QR が 1 つだけのとき detectedCount は 1")
    func reportsSingleDetection() async throws {
        let decoded = try await VisionQRDecoder().decode(QRImageFixture.qr("only one"))
        #expect(decoded?.detectedCount == 1)
    }

    @Test("QR の無い画像では nil を返す")
    func returnsNilForBlankImage() async throws {
        let blank = try QRImageFixture.whiteCanvas(width: 200, height: 200)
        let decoded = try await VisionQRDecoder().decode(blank)
        #expect(decoded == nil)
    }

    @Test("バイナリペイロードの QR だけでは nil を返す")
    func returnsNilForBinaryOnlyQR() async throws {
        let binary = try QRImageFixture.qr(
            Data([0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87]), scale: 8)
        let decoded = try await VisionQRDecoder().decode(binary)
        #expect(decoded == nil)
    }

    // QR を 3 つ置くのは、この 1 件だけで「先頭を取っただけ」と「昇順に並べただけ」の
    // 両方を落とすため。2 つだと昇順ソートでも SMALL が返って通ってしまう。
    @Test("バイナリペイロードの QR が最大でも、読み取れる次の候補を採用する")
    func skipsUnreadableBinaryQRForNextCandidate() async throws {
        let big = try QRImageFixture.qr(Data([0x80, 0x81, 0x82, 0x83, 0x84, 0x85, 0x86, 0x87]), scale: 8)
        let medium = try QRImageFixture.qr("MEDIUM", scale: 5)
        let small = try QRImageFixture.qr("SMALL", scale: 3)
        let canvas = try QRImageFixture.whiteCanvas(
            width: 400,
            height: 240,
            placing: [
                (big, CGPoint(x: 8, y: 8)),
                (medium, CGPoint(x: 220, y: 8)),
                (small, CGPoint(x: 300, y: 150)),
            ]
        )

        let decoded = try await VisionQRDecoder().decode(canvas)

        #expect(decoded?.payload == "MEDIUM")
        #expect(decoded?.detectedCount == 3)
    }
}
```

バイナリペイロードのバイト列選びには落とし穴がある。Vision の byte → string 変換は純粋な UTF-8 検証より寛容で、`Data(repeating: 0xFF, count:)` は Latin-1 として `"ÿÿÿ..."` にデコードされてしまい `payloadString` が nil にならない。上記は先行バイトのない継続バイトだけを並べたもので、この経路を確実に nil にできることを実測で確認してある。

- [ ] **Step 3: テストを実行して失敗を確認する**

Run: `swift test`

Expected: コンパイルエラー。`cannot find 'VisionQRDecoder' in scope`。Swift は静的型付けなので、これがこのループの red 状態。

- [ ] **Step 4: 実装を書く**

`Sources/QRShotKit/QRDecoder.swift`:

```swift
import CoreGraphics
import Vision

/// デコードに成功した QR コード 1 件と、同じ画像から見つかった総数。
public struct DecodedQR: Equatable, Sendable {
    public let payload: String
    public let detectedCount: Int

    public init(payload: String, detectedCount: Int) {
        self.payload = payload
        self.detectedCount = detectedCount
    }
}

/// 画像から QR コードを読み取る。
public protocol QRDecoder {
    /// QR が無い、または文字列として読み取れない場合は nil を返す。
    func decode(_ image: CGImage) async throws -> DecodedQR?
}

/// Vision による実装。バウンディングボックスの面積が最大の QR を優先して採用する。
public struct VisionQRDecoder: QRDecoder {
    public init() {}

    public func decode(_ image: CGImage) async throws -> DecodedQR? {
        var request = DetectBarcodesRequest()
        request.symbologies = [.qr]
        let observations = try await request.perform(on: image)

        let largestFirst = observations.sorted { area(of: $0) > area(of: $1) }
        guard let payload = largestFirst.lazy.compactMap(\.payloadString).first else {
            return nil
        }
        return DecodedQR(payload: payload, detectedCount: observations.count)
    }

    private func area(of observation: BarcodeObservation) -> Double {
        observation.boundingBox.width * observation.boundingBox.height
    }
}
```

- [ ] **Step 5: テストを実行して成功を確認する**

Run: `swift test`

Expected: `Test run with 8 tests in 1 suite passed`

- [ ] **Step 6: コミット**

```bash
git add Sources/QRShotKit/QRDecoder.swift Tests/QRShotKitTests/QRImageFixture.swift Tests/QRShotKitTests/VisionQRDecoderTests.swift
git commit -m "✨ Add Vision-backed QR decoder"
```

---

### Task 3: クリップボード

**Files:**
- Create: `Tests/QRShotKitTests/PasteboardClipboardTests.swift`
- Create: `Sources/QRShotKit/Clipboard.swift`

`NSPasteboard` を注入可能にすることで、`NSPasteboard.withUniqueName()` を使ってユーザーの実クリップボードを壊さずにテストできる。

- [ ] **Step 1: 失敗するテストを書く**

`Tests/QRShotKitTests/PasteboardClipboardTests.swift`:

```swift
import AppKit
import Testing

@testable import QRShotKit

@Suite("PasteboardClipboard")
struct PasteboardClipboardTests {
    @Test("書き込んだ文字列が読み返せる")
    func writesString() throws {
        let pasteboard = NSPasteboard.withUniqueName()
        defer { pasteboard.releaseGlobally() }

        try PasteboardClipboard(pasteboard: pasteboard).copy("コピーされる文字列")

        #expect(pasteboard.string(forType: .string) == "コピーされる文字列")
    }

    @Test("2 回書き込むと後のものだけが残る")
    func replacesPreviousContents() throws {
        let pasteboard = NSPasteboard.withUniqueName()
        defer { pasteboard.releaseGlobally() }
        let clipboard = PasteboardClipboard(pasteboard: pasteboard)

        try clipboard.copy("さいしょ")
        try clipboard.copy("あと")

        #expect(pasteboard.string(forType: .string) == "あと")
    }
}
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run: `swift test`

Expected: コンパイルエラー。`cannot find 'PasteboardClipboard' in scope`

- [ ] **Step 3: 実装を書く**

`Sources/QRShotKit/Clipboard.swift`:

```swift
import AppKit

/// 文字列をクリップボードへ書き込む。
public protocol Clipboard {
    func copy(_ text: String) throws
}

public enum ClipboardError: Error, CustomStringConvertible {
    case writeFailed

    public var description: String {
        "クリップボードに書き込めませんでした"
    }
}

/// `NSPasteboard` による実装。テストでは一時ペーストボードを注入する。
public struct PasteboardClipboard: Clipboard {
    private let pasteboard: NSPasteboard

    public init(pasteboard: NSPasteboard = .general) {
        self.pasteboard = pasteboard
    }

    public func copy(_ text: String) throws {
        pasteboard.clearContents()
        guard pasteboard.setString(text, forType: .string) else {
            throw ClipboardError.writeFailed
        }
    }
}
```

- [ ] **Step 4: テストを実行して成功を確認する**

Run: `swift test`

Expected: `Test run with 10 tests in 2 suites passed`

- [ ] **Step 5: コミット**

```bash
git add Sources/QRShotKit/Clipboard.swift Tests/QRShotKitTests/PasteboardClipboardTests.swift
git commit -m "✨ Add NSPasteboard-backed clipboard"
```

---

### Task 4: 通知

**Files:**
- Create: `Tests/QRShotKitTests/TerminalNotifierTests.swift`
- Create: `Sources/QRShotKit/Notifier.swift`

`notify` 自体は通知センターの目視が必要なので自動テストしない。代わりに、PATH から実行ファイルを探す `locate` を注入可能な `path` 引数付きにして、そこだけテストする。

- [ ] **Step 1: 失敗するテストを書く**

`Tests/QRShotKitTests/TerminalNotifierTests.swift`:

```swift
import Foundation
import Testing

@testable import QRShotKit

@Suite("TerminalNotifier.locate")
struct TerminalNotifierLocateTests {
    /// PATH の 1 要素として使える一時ディレクトリを作る。
    /// `executable` が true のとき、その中に実行可能な terminal-notifier を置く。
    private func makeDirectory(containingExecutable executable: Bool) throws -> URL {
        let directory = FileManager.default.temporaryDirectory
            .appendingPathComponent("qrshot-locate-\(UUID().uuidString)")
        try FileManager.default.createDirectory(at: directory, withIntermediateDirectories: true)
        if executable {
            let binary = directory.appendingPathComponent("terminal-notifier")
            try Data("#!/bin/sh\n".utf8).write(to: binary)
            try FileManager.default.setAttributes(
                [.posixPermissions: 0o755], ofItemAtPath: binary.path)
        }
        return directory
    }

    @Test("PATH 上に実行可能な terminal-notifier があれば見つかる")
    func findsExecutableOnPath() throws {
        let directory = try makeDirectory(containingExecutable: true)
        defer { try? FileManager.default.removeItem(at: directory) }

        #expect(TerminalNotifier.locate(path: directory.path) != nil)
    }

    @Test("PATH 上に無ければ nil")
    func returnsNilWhenAbsent() throws {
        let directory = try makeDirectory(containingExecutable: false)
        defer { try? FileManager.default.removeItem(at: directory) }

        #expect(TerminalNotifier.locate(path: directory.path) == nil)
    }

    @Test("PATH が空なら nil")
    func returnsNilForEmptyPath() {
        #expect(TerminalNotifier.locate(path: "") == nil)
    }

    @Test("PATH の後ろのディレクトリにあっても見つかる")
    func searchesEveryPathEntry() throws {
        let empty = try makeDirectory(containingExecutable: false)
        let populated = try makeDirectory(containingExecutable: true)
        defer {
            try? FileManager.default.removeItem(at: empty)
            try? FileManager.default.removeItem(at: populated)
        }

        #expect(TerminalNotifier.locate(path: "\(empty.path):\(populated.path)") != nil)
    }
}
```

- [ ] **Step 2: テストを実行して失敗を確認する**

Run: `swift test`

Expected: コンパイルエラー。`cannot find 'TerminalNotifier' in scope`

- [ ] **Step 3: 実装を書く**

通知メッセージは設計書の文言そのまま。`Process` に引数を配列で渡すのでシェルを経由せず、デコード結果に何が入っていてもエスケープ事故が起きない。

`Sources/QRShotKit/Notifier.swift`:

```swift
import Foundation

public enum NotificationOutcome: Equatable, Sendable {
    case success
    case failure

    public var message: String {
        switch self {
        case .success: "QRコードの読み込みに成功しました"
        case .failure: "QRコードが存在しないまたは読み取れない状態にあります"
        }
    }
}

/// 結果をユーザーに通知する。
public protocol Notifier {
    func notify(_ outcome: NotificationOutcome) throws
}

public enum NotifierError: Error, CustomStringConvertible {
    case launchFailed(String)

    public var description: String {
        switch self {
        case .launchFailed(let reason):
            return "terminal-notifier を実行できませんでした: \(reason)"
        }
    }
}

/// `terminal-notifier` を呼ぶ実装。
public struct TerminalNotifier: Notifier {
    public static let installHint = """
        terminal-notifier が見つかりません。次のコマンドでインストールしてください:
          brew install terminal-notifier
        """

    private let executableURL: URL

    public init(executableURL: URL) {
        self.executableURL = executableURL
    }

    /// PATH から terminal-notifier を探す。見つからなければ nil。
    public static func locate(
        path: String = ProcessInfo.processInfo.environment["PATH"] ?? ""
    ) -> TerminalNotifier? {
        for directory in path.split(separator: ":") {
            let candidate = URL(fileURLWithPath: String(directory))
                .appendingPathComponent("terminal-notifier")
            if FileManager.default.isExecutableFile(atPath: candidate.path) {
                return TerminalNotifier(executableURL: candidate)
            }
        }
        return nil
    }

    public func notify(_ outcome: NotificationOutcome) throws {
        let process = Process()
        process.executableURL = executableURL
        process.arguments = ["-title", "qrshot", "-message", outcome.message]
        do {
            try process.run()
        } catch {
            throw NotifierError.launchFailed(error.localizedDescription)
        }
        process.waitUntilExit()
    }
}
```

- [ ] **Step 4: テストを実行して成功を確認する**

Run: `swift test`

Expected: `Test run with 14 tests in 3 suites passed`

- [ ] **Step 5: コミット**

```bash
git add Sources/QRShotKit/Notifier.swift Tests/QRShotKitTests/TerminalNotifierTests.swift
git commit -m "✨ Add terminal-notifier backed notifier"
```

---

### Task 5: 画面キャプチャ

**Files:**
- Create: `Sources/QRShotKit/ScreenCapturer.swift`

このタスクだけ自動テストが無い。領域選択は人間の操作を要求するため。Task 8 の手動確認で担保する。

キャンセル判定を終了コードではなくファイルの有無で行うのが要点。Esc 時の `screencapture` の終了コードは macOS のバージョンによって揺れる。

`Process` はデフォルトで親の stderr を継承するので、`screencapture` が画面収録権限などについて出力した内容はそのままユーザーの目に入る。追加のコードは要らない。

- [ ] **Step 1: 実装を書く**

`Sources/QRShotKit/ScreenCapturer.swift`:

```swift
import CoreGraphics
import Foundation
import ImageIO

/// 画面の領域選択を行い、キャプチャした画像を返す。
public protocol ScreenCapturer {
    /// ユーザーが選択をキャンセルした場合は nil を返す。
    func capture() throws -> CGImage?
}

public enum ScreenCaptureError: Error, CustomStringConvertible {
    case launchFailed(String)
    case unreadableImage

    public var description: String {
        switch self {
        case .launchFailed(let reason):
            return "screencapture を実行できませんでした: \(reason)"
        case .unreadableImage:
            return "スクリーンショットを画像として読み込めませんでした"
        }
    }
}

/// `screencapture -i` で領域選択を行う実装。
public struct InteractiveScreenCapturer: ScreenCapturer {
    public init() {}

    public func capture() throws -> CGImage? {
        let url = FileManager.default.temporaryDirectory
            .appendingPathComponent("qrshot-\(UUID().uuidString).png")
        defer { try? FileManager.default.removeItem(at: url) }

        let process = Process()
        process.executableURL = URL(fileURLWithPath: "/usr/sbin/screencapture")
        process.arguments = ["-i", url.path]
        do {
            try process.run()
        } catch {
            throw ScreenCaptureError.launchFailed(error.localizedDescription)
        }
        process.waitUntilExit()

        // Esc でキャンセルしたときの終了コードは macOS のバージョンで揺れるため、
        // 出力ファイルの有無で判定する。
        guard FileManager.default.fileExists(atPath: url.path) else { return nil }

        guard let source = CGImageSourceCreateWithURL(url as CFURL, nil),
              let image = CGImageSourceCreateImageAtIndex(source, 0, nil)
        else {
            throw ScreenCaptureError.unreadableImage
        }
        return image
    }
}
```

- [ ] **Step 2: ビルドを確認する**

Run: `swift build`

Expected: `Build complete!`。警告なし。

- [ ] **Step 3: 既存テストが壊れていないことを確認する**

Run: `swift test`

Expected: `Test run with 14 tests in 3 suites passed`

- [ ] **Step 4: コミット**

```bash
git add Sources/QRShotKit/ScreenCapturer.swift
git commit -m "✨ Add interactive screen capturer"
```

---

### Task 6: オーケストレータ

**Files:**
- Create: `Tests/QRShotKitTests/Fakes.swift`
- Create: `Tests/QRShotKitTests/QRShotTests.swift`
- Create: `Sources/QRShotKit/QRShot.swift`

クリップボード書き込みが失敗したときに成功通知を出さないこと（設計書の「exit 2、通知なし」に対応）まで含めてテストする。

- [ ] **Step 1: fake を書く**

`Tests/QRShotKitTests/Fakes.swift`:

```swift
import CoreGraphics
import Foundation

@testable import QRShotKit

struct FakeError: Error, Equatable {
    let message: String
}

final class StubScreenCapturer: ScreenCapturer {
    private let image: CGImage?
    private let error: (any Error)?

    init(image: CGImage?, error: (any Error)? = nil) {
        self.image = image
        self.error = error
    }

    func capture() throws -> CGImage? {
        if let error { throw error }
        return image
    }
}

final class StubQRDecoder: QRDecoder {
    private let result: DecodedQR?

    init(result: DecodedQR?) {
        self.result = result
    }

    func decode(_ image: CGImage) async throws -> DecodedQR? {
        result
    }
}

final class SpyClipboard: Clipboard {
    private(set) var copied: [String] = []
    var failure: (any Error)?

    func copy(_ text: String) throws {
        if let failure { throw failure }
        copied.append(text)
    }
}

final class SpyNotifier: Notifier {
    private(set) var notified: [NotificationOutcome] = []

    func notify(_ outcome: NotificationOutcome) throws {
        notified.append(outcome)
    }
}
```

- [ ] **Step 2: 失敗するテストを書く**

`Tests/QRShotKitTests/QRShotTests.swift`:

```swift
import Testing

@testable import QRShotKit

@Suite("QRShot")
struct QRShotTests {
    private func makeQRShot(
        capturer: StubScreenCapturer,
        decoder: StubQRDecoder,
        clipboard: SpyClipboard,
        notifier: SpyNotifier
    ) -> QRShot {
        QRShot(capturer: capturer, decoder: decoder, clipboard: clipboard, notifier: notifier)
    }

    @Test("キャンセル時はクリップボードにも通知にも触らない")
    func cancelTouchesNothing() async throws {
        let clipboard = SpyClipboard()
        let notifier = SpyNotifier()
        let qrshot = makeQRShot(
            capturer: StubScreenCapturer(image: nil),
            decoder: StubQRDecoder(result: DecodedQR(payload: "unused", detectedCount: 1)),
            clipboard: clipboard,
            notifier: notifier
        )

        let outcome = try await qrshot.run()

        #expect(outcome == .cancelled)
        #expect(clipboard.copied.isEmpty)
        #expect(notifier.notified.isEmpty)
    }

    @Test("読み取り成功時はコピー 1 回と成功通知 1 回")
    func successCopiesAndNotifies() async throws {
        let clipboard = SpyClipboard()
        let notifier = SpyNotifier()
        let decoded = DecodedQR(payload: "https://example.com", detectedCount: 1)
        let qrshot = makeQRShot(
            capturer: StubScreenCapturer(image: try QRImageFixture.qr("unused")),
            decoder: StubQRDecoder(result: decoded),
            clipboard: clipboard,
            notifier: notifier
        )

        let outcome = try await qrshot.run()

        #expect(outcome == .copied(decoded))
        #expect(clipboard.copied == ["https://example.com"])
        #expect(notifier.notified == [.success])
    }

    @Test("読み取り失敗時はコピーせず失敗通知だけ出す")
    func failureNotifiesWithoutCopying() async throws {
        let clipboard = SpyClipboard()
        let notifier = SpyNotifier()
        let qrshot = makeQRShot(
            capturer: StubScreenCapturer(image: try QRImageFixture.qr("unused")),
            decoder: StubQRDecoder(result: nil),
            clipboard: clipboard,
            notifier: notifier
        )

        let outcome = try await qrshot.run()

        #expect(outcome == .notFound)
        #expect(clipboard.copied.isEmpty)
        #expect(notifier.notified == [.failure])
    }

    @Test("クリップボード書き込みが失敗したら成功通知を出さない")
    func clipboardFailureSuppressesSuccessNotification() async throws {
        let clipboard = SpyClipboard()
        clipboard.failure = FakeError(message: "書き込み不可")
        let notifier = SpyNotifier()
        let qrshot = makeQRShot(
            capturer: StubScreenCapturer(image: try QRImageFixture.qr("unused")),
            decoder: StubQRDecoder(result: DecodedQR(payload: "text", detectedCount: 1)),
            clipboard: clipboard,
            notifier: notifier
        )

        await #expect(throws: FakeError.self) {
            try await qrshot.run()
        }
        #expect(notifier.notified.isEmpty)
    }
}
```

- [ ] **Step 3: テストを実行して失敗を確認する**

Run: `swift test`

Expected: コンパイルエラー。`cannot find 'QRShot' in scope`

- [ ] **Step 4: 実装を書く**

`Sources/QRShotKit/QRShot.swift`:

```swift
/// 領域選択からクリップボードへのコピーまでの流れを組み立てる。
public struct QRShot {
    public enum Outcome: Equatable, Sendable {
        case copied(DecodedQR)
        case notFound
        case cancelled
    }

    private let capturer: any ScreenCapturer
    private let decoder: any QRDecoder
    private let clipboard: any Clipboard
    private let notifier: any Notifier

    public init(
        capturer: any ScreenCapturer,
        decoder: any QRDecoder,
        clipboard: any Clipboard,
        notifier: any Notifier
    ) {
        self.capturer = capturer
        self.decoder = decoder
        self.clipboard = clipboard
        self.notifier = notifier
    }

    public func run() async throws -> Outcome {
        guard let image = try capturer.capture() else { return .cancelled }
        guard let decoded = try await decoder.decode(image) else {
            try notifier.notify(.failure)
            return .notFound
        }
        try clipboard.copy(decoded.payload)
        try notifier.notify(.success)
        return .copied(decoded)
    }
}
```

- [ ] **Step 5: テストを実行して成功を確認する**

Run: `swift test`

Expected: `Test run with 18 tests in 4 suites passed`

- [ ] **Step 6: コミット**

```bash
git add Sources/QRShotKit/QRShot.swift Tests/QRShotKitTests/Fakes.swift Tests/QRShotKitTests/QRShotTests.swift
git commit -m "✨ Add QRShot orchestrator"
```

---

### Task 7: CLI エントリポイント

**Files:**
- Replace: `Sources/qrshot/main.swift`（Task 1 で置いたコメントだけのプレースホルダを、下記の内容で丸ごと置き換える）

`main.swift` の top-level code で `await` が使える。`@main` 構造体は不要。

exit code の対応は設計書の表のとおり: 成功とキャンセルが 0、QR 読み取り失敗が 1、環境エラーが 2。

- [ ] **Step 1: 実装を書く**

`Sources/qrshot/main.swift`:

```swift
import Foundation
import QRShotKit

let version = "0.1.0"
let usage = """
    qrshot - 画面の領域を選択して、写っている QR コードをクリップボードへコピーします

    使い方:
      qrshot            領域選択を開始する
      qrshot --help     このヘルプを表示する
      qrshot --version  バージョンを表示する

    実行には terminal-notifier が必要です:
      brew install terminal-notifier
    """

func printToStandardError(_ message: String) {
    FileHandle.standardError.write(Data((message + "\n").utf8))
}

func fail(_ message: String) -> Never {
    printToStandardError(message)
    exit(2)
}

let arguments = Array(CommandLine.arguments.dropFirst())

if arguments.contains("--help") || arguments.contains("-h") {
    print(usage)
    exit(0)
}

if arguments.contains("--version") {
    print("qrshot \(version)")
    exit(0)
}

guard arguments.isEmpty else {
    fail("不明な引数: \(arguments.joined(separator: " "))\n\n\(usage)")
}

guard let notifier = TerminalNotifier.locate() else {
    fail(TerminalNotifier.installHint)
}

let qrshot = QRShot(
    capturer: InteractiveScreenCapturer(),
    decoder: VisionQRDecoder(),
    clipboard: PasteboardClipboard(),
    notifier: notifier
)

do {
    switch try await qrshot.run() {
    case .cancelled:
        exit(0)
    case .copied(let decoded):
        if decoded.detectedCount > 1 {
            printToStandardError(
                "QR コードを \(decoded.detectedCount) 個検出したので、読み取れたうち一番大きいものを使いました")
        }
        exit(0)
    case .notFound:
        exit(1)
    }
} catch {
    fail("\(error)")
}
```

- [ ] **Step 2: ビルドする**

Run: `swift build`

Expected: `Build complete!`。警告なし。

- [ ] **Step 3: 領域選択を起動しない経路を確認する**

Run:

```bash
./.build/debug/qrshot --version
```

Expected: `qrshot 0.1.0`

Run:

```bash
./.build/debug/qrshot --help
```

Expected: usage が表示され、exit code は 0

Run:

```bash
./.build/debug/qrshot --bogus; echo "exit=$?"
```

Expected: stderr に `不明な引数: --bogus` と usage が出て `exit=2`

- [ ] **Step 4: terminal-notifier 不在時の挙動を確認する**

PATH を空にして、領域選択が始まる前に落ちることを確認する。

Run:

```bash
env PATH= ./.build/debug/qrshot; echo "exit=$?"
```

Expected: `terminal-notifier が見つかりません。次のコマンドでインストールしてください:` と `brew install terminal-notifier` が stderr に出て `exit=2`。**領域選択の十字カーソルは出ない。**

- [ ] **Step 5: テストが通ることを再確認する**

Run: `swift test`

Expected: `Test run with 18 tests in 4 suites passed`

- [ ] **Step 6: コミット**

```bash
git add Sources/qrshot/main.swift
git commit -m "✨ Add qrshot CLI entry point"
```

---

### Task 8: README と手動確認

**Files:**
- Create: `README.md`

自動テストできない 2 つ（`InteractiveScreenCapturer` と `TerminalNotifier.notify`）を、ここで実際に手を動かして確認する。

- [ ] **Step 1: `README.md` を書く**

````markdown
# qrshot

画面の領域を選択して、そこに写っている QR コードをクリップボードへコピーする macOS 用 CLI ツール。

## 必要なもの

- macOS 15 以上
- Swift 6 以上（Xcode 同梱のもので可）
- [terminal-notifier](https://github.com/julienXX/terminal-notifier)

```bash
brew install terminal-notifier
```

## インストール

```bash
swift build -c release
cp .build/release/qrshot /usr/local/bin/
```

## 使い方

```bash
qrshot
```

十字カーソルが出るのでドラッグして範囲を選ぶ。スペースキーでウィンドウ選択に切り替わる。Esc でキャンセル。

選択範囲に QR コードがあれば、その中身がクリップボードに入り「QRコードの読み込みに成功しました」と通知される。読み取れなかった場合は「QRコードが存在しないまたは読み取れない状態にあります」と通知される。

QR コードが複数写っていた場合は、一番大きく写っているものを採用する。

## 終了コード

| コード | 意味 |
|---|---|
| 0 | 読み取り成功、またはユーザーによるキャンセル |
| 1 | QR コードが無い、または読み取れなかった |
| 2 | terminal-notifier が無い、`screencapture` を起動できない、クリップボードに書き込めないなどの環境エラー |

## 画面収録の許可について

初回実行時、macOS が画面収録の許可を求めることがある。許可していない状態では `screencapture` が画像を作らずに終了するため、qrshot からはキャンセルと区別がつかず、何も起きずに終了する。何も起きない場合は「システム設定 > プライバシーとセキュリティ > 画面収録」で、qrshot を起動しているターミナルアプリに許可を与えること。

## 開発

```bash
swift build
swift test
```

### 手動確認

自動テストできない部分（領域選択そのものと、通知センターへの表示）は手で確認する。

1. **読み取り成功**: 適当な QR コードを画面に表示して `qrshot` を実行し、その QR を囲む。「QRコードの読み込みに成功しました」の通知が出て、`pbpaste` で中身が取り出せること
2. **読み取り失敗**: QR コードの無い領域を囲む。「QRコードが存在しないまたは読み取れない状態にあります」の通知が出て、`echo $?` が `1` を返すこと
3. **キャンセル**: `qrshot` を実行して Esc を押す。通知が出ず、`echo $?` が `0` を返し、クリップボードの中身が変わっていないこと
4. **ウィンドウ選択**: `qrshot` を実行してスペースキーを押し、QR コードが表示されているウィンドウをクリック。読み取れること
5. **複数検出**: QR コードが 2 つ以上写る範囲を囲む。大きいほうの中身がコピーされ、stderr に「QR コードを N 個検出したので、読み取れたうち一番大きいものを使いました」と出ること
````

- [ ] **Step 2: 手動確認 1 — 読み取り成功**

適当な QR コードを画面に表示する（例: `https://example.com` の QR を検索して表示、または任意の QR 画像を開く）。

Run: `./.build/debug/qrshot`

十字カーソルで QR を囲む。

Expected: 「QRコードの読み込みに成功しました」の通知が出る。続けて `pbpaste` を実行すると QR の中身が出る。`echo $?` が `0`。

- [ ] **Step 3: 手動確認 2 — 読み取り失敗**

Run: `./.build/debug/qrshot; echo "exit=$?"`

QR コードの無い、無地の領域を囲む。

Expected: 「QRコードが存在しないまたは読み取れない状態にあります」の通知が出て `exit=1`。

- [ ] **Step 4: 手動確認 3 — キャンセル**

まず `echo "canary" | pbcopy` でクリップボードに目印を入れておく。

Run: `./.build/debug/qrshot; echo "exit=$?"`

十字カーソルが出たら Esc を押す。

Expected: 通知が出ず `exit=0`。`pbpaste` が `canary` を返す（クリップボードが触られていない）。

- [ ] **Step 5: 手動確認 4 — ウィンドウ選択**

Run: `./.build/debug/qrshot`

十字カーソルが出たらスペースキーを押し、QR コードが表示されているウィンドウをクリックする。

Expected: 読み取りに成功し、成功通知が出る。

- [ ] **Step 6: 手動確認 5 — 複数検出**

QR コードを大小 2 つ画面に並べる（大きさが明確に違うもの）。

Run: `./.build/debug/qrshot`

両方が入る範囲を囲む。

Expected: 大きいほうの中身が `pbpaste` で取れる。stderr に「QR コードを 2 個検出したので、読み取れたうち一番大きいものを使いました」が出る。

- [ ] **Step 7: コミット**

```bash
git add README.md
git commit -m "📝 Add README with usage and manual verification steps"
```

---

## 完了条件

- [ ] `swift build` が警告なしで通る
- [ ] `swift test` が 18 件すべて通る
- [ ] Task 8 の手動確認 5 項目がすべて期待どおり

### 設計書の「終了コードと出口の一覧」との対応

| 設計書の行 | 確認手段 |
|---|---|
| QR 読み取り成功 → exit 0 | Task 8 手動確認 1 + `QRShotTests.successCopiesAndNotifies` |
| QR 複数検出 → exit 0 + stderr 注記 | Task 8 手動確認 5 + `VisionQRDecoderTests.prefersLargestQR` |
| Esc でキャンセル → exit 0 | Task 8 手動確認 3 + `QRShotTests.cancelTouchesNothing` |
| QR が無い / 読めない → exit 1 | Task 8 手動確認 2 + `QRShotTests.failureNotifiesWithoutCopying` |
| `terminal-notifier` が無い → exit 2 | Task 7 Step 4（`env PATH=` で再現） |
| `screencapture` が起動できない → exit 2 | **直接の確認手段なし。** `/usr/sbin/screencapture` は SIP 配下で消せず、起動失敗を人為的に作れない。`InteractiveScreenCapturer.capture()` の `catch` がコードレビューで正しいことを確認するに留める |
| クリップボード書き込み失敗 → exit 2 | `QRShotTests.clipboardFailureSuppressesSuccessNotification`（オーケストレータが throw することまで。exit code への変換は `main.swift` の `catch` で、これも直接の確認手段はない） |
